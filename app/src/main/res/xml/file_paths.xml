<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Downloaded extension APKs live under cacheDir/extensions and are shared
         with the system package installer via FileProvider. -->
    <cache-path name="extensions" path="extensions/" />
</paths>
